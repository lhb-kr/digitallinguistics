# Shakespeare Character Networks Web App

This is a static first version of the analysis interface. It reads the precomputed JSON files in `data/processed` and renders the current play snapshot on a canvas.

Run it from the project root:

```bash
python3 -m http.server 8000
```

Then open:

```text
http://localhost:8000/web/
```
